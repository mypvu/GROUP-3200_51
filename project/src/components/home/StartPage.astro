---
import SidebarButton from "./sidebar/SidebarButton.astro";
---

<div
    class="flex flex-col gap-10 min-w-screen min-h-screen p-10 justify-center items-center"
>
    <!-- Title -->
    <div class="text-5xl font-bold">HTPLC Compound Matcher</div>

    <!-- Content -->
    <div class="flex flex-row flex-wrap gap-10 justify-center">
        <!-- Sidebar (left side with the 3 buttons) -->
        <div class="flex flex-col justify-between gap-2">
            <SidebarButton title="Incomplete Analyse" />
            <SidebarButton title="View Database" />
            <SidebarButton title="Previous Analyse" />
        </div>

        <!-- Main (right side with the start button) -->
        <div>
            <div
                class="flex flex-col w-100 h-100 justify-center items-center gap-5 aspect-square border-black border-4 rounded-3xl"
            >
                <div class="text-3xl font-bold">Start New Analysis</div>
                <div
                    class="p-5 w-50 border-black border-4 rounded-3xl text-2xl font-bold text-center"
                >
                    →
                </div>
            </div>
        </div>
    </div>
</div>
