---
interface Props {
  href?: string;
  onClick?: () => void;
  text: string;
}
const { href, onClick, text } = Astro.props;
---

<a 
  href={href} 
  onClick={onClick} 
  class="inline-block px-4 py-2 rounded-2xl border-2 border-black hover:bg-black hover:text-white transition">
  {text}
</a>
